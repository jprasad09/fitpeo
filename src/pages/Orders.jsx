import React from 'react';
import withLayout from '../utils/withLayout';

const Orders = () => {
  return (
    <section className='text-[--primary-text-color] p-5 h-full w-full flex items-center justify-center text-3xl'>
      Orders
    </section>
  )
}

export default withLayout(Orders);